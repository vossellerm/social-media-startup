const router = require("express").Router();

// GET all users
// GET single user by _id && thought and friend data
// POST new user
// PUT update user by _id
// DELETE remove iser by _id

module.exports = router;
